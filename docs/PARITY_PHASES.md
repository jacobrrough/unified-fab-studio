# CAD parity phases (implementation tracker)

Work proceeds **one phase at a time**. This file records what is done vs next.

**Detailed backlog (epics, catalog IDs, exit criteria, milestones):** [`PARITY_REMAINING_ROADMAP.md`](PARITY_REMAINING_ROADMAP.md).

| Phase | Theme | Status |
|-------|--------|--------|
| **1** | Geometry foundation — B-rep path from design, kernel manifest, CadQuery STEP+STL | **Done** — see `GEOMETRY_KERNEL.md`, `build_part.py`, **Design → Build STEP (kernel)** |
| **2** | Sketch depth — lines, arcs, trim, constraints, annotation | **Done (v2 baseline)** — **Line** tool (two-point open polyline); **construction Point** tool (click → `points` map entry, no entity); **regular polygon** (closed point-ID polyline, 3–128 sides); **rounded slot** (`slot` entity; center–center or overall tip length + width); **polyline / rect / circle** (center+radius, **two diameter points**, or **three-point circumcircle**) **/ 3pt arc** + **center–start–end arc** (minor arc, stored as three-point `arc`); **three-point rectangle**; **trim** + **sketch fillet** + **sketch chamfer** (corner cut, leg length L); **extend / break / split**; **offset** closed point-ID loop (miter, ribbon); constraints: coincident, distance, H/V, fix, perpendicular, parallel, equal, collinear, midpoint, angle, **tangent**, **symmetric**, **concentric**, **radius/diameter**; **linear dimension** annotations (mm on canvas, non-driving); driving parameters ribbon; further polish (splines, driving dims, arc–arc fillet) → [`PARITY_REMAINING_ROADMAP.md`](PARITY_REMAINING_ROADMAP.md) §Phase 2 + [`STRETCH_SCOPE.md`](STRETCH_SCOPE.md) |
| **3** | Solid modeling on B-rep — fillet, boolean, shell, patterns on kernel bodies | **Done (kernel baseline + exit samples)** — same shipped `kernelOps` as before; **complete `part/*.example.json` set** + `design/sketch.rect-circle.example.json` for profile-index ops; Vitest **`sample-kernel-solid-ops-examples.test.ts`** guards schema drift. **Stretch** (Fusion-depth UX / true sweep–thread–thicken) → [`PARITY_REMAINING_ROADMAP.md`](PARITY_REMAINING_ROADMAP.md) §Phase 3 stretch |
| **4** | Surfaces / sheet metal / plastic | **Done (kernel + preview baseline)** — **Loft:** 2–16 closed profiles in entity order, uniform **`loftSeparationMm`**; Three.js ruled strips; kernel **union-chain** of segment lofts + **`loftStrategy`** (`multi+union-chain:…` when *n*≥3); sample [`resources/sample-kernel-loft-multi/`](../resources/sample-kernel-loft-multi/README.md). **Sheet tab:** **`sheet_tab_union`** + Design ribbon **+ sheet tab**; sample [`resources/sample-kernel-sheet-tab/`](../resources/sample-kernel-sheet-tab/README.md). **Command catalog:** **`so_loft`** / **`su_loft`** → **implemented**; **`sm_flange`** stays **partial** (axis-aligned tab, no bend metadata). **Not** in scope: path-guided surfacing, **bends / flat pattern**, plastic rule features — see [`PARITY_REMAINING_ROADMAP.md`](PARITY_REMAINING_ROADMAP.md) §Phase 4 stretch |
| **5** | Assembly — joints, references, interference | **Done (baseline + stretch slice)** — same as before; **interference** report **download** or **save** `output/{assembly}-interference.json`; **BOM** flat CSV (`buildAssemblyBomCsvLines`: **`instanceId`**, optional **`bomUnit` / `bomVendor` / `bomCostEach`**, motion-link stubs, …) + **`output/bom-hierarchical.txt`** + **`output/bom-hierarchy.json`**. **BOM preview** table adds **Thumb** (STL raster + **cache** + **IntersectionObserver** lazy load, not in CSV; **no** separate thumbnail IPC — see [`VERIFICATION.md`](VERIFICATION.md)). **Joint / motion UX:** DOF hints + motion-link validation; **revolute** + **slider** + **universal** + **cylindrical** preview stubs (world/parent-local axes where applicable; motion study **whole-assembly** +Y rotation **after** joint previews in the viewport); **Duplicate row**; **Insert from project…** (JSON under project, **relative `partPath`** + `dialog:openFile` with `defaultPath`). **Remaining stretch:** true multibody kinematics / joint limits beyond preview stubs — [`PARITY_REMAINING_ROADMAP.md`](PARITY_REMAINING_ROADMAP.md) §Phase 5 |
| **6** | Manufacture — deeper CAM, simulation | **Done (baseline)** — same shipped stack as above (setups/stock/WCS, **`cnc_parallel`** + OCL **`cnc_waterline`** / **`cnc_adaptive`** / **`cnc_raster`** with fallbacks, and built-in 2D **`cnc_contour`** / **`cnc_pocket`** / **`cnc_drill`** from operation geometry with hard validation + machine-aware drill cycle behavior, no STL fallback for missing/invalid 2D geometry); 2D / empty-toolpath failures return **human-readable `error` + optional `hint`** over **`cam:run`** (Make tab shows both). **`cam:run`** + **`cam-cut-params`** + [`cnc_generic_mm.hbs`](../resources/posts/cnc_generic_mm.hbs) **G54–G59**; tests in **`cam-*.test.ts`**, **`cam-operation-policy`**, **`manufacture-schema`**. **Stretch (partial):** Manufacture tab **simulation stub** (`mf_simulate`); deeper 2D cycles, real stock-removal sim, deeper slicer — [`PARITY_REMAINING_ROADMAP.md`](PARITY_REMAINING_ROADMAP.md) §Phase 6 |
| **7** | Product polish — command palette, drawings, configs | **Done (baseline)** — command palette (**workspace filter**, default **implemented-only**) + **`ut_command_palette`** (**`aria-modal`**, **Tab wrap**, **Home/End**, **scrollIntoView**, **`aria-describedby`**); **Shortcuts** (**`ut_keyboard_shortcuts`**, **Ctrl+Shift+?** / **⌘⇧?** when not in a typable field) from **`app-keyboard-shortcuts.ts`** + [`KEYBOARD_SHORTCUTS.md`](KEYBOARD_SHORTCUTS.md); **`ShellStatusFooter`** + dismissible palette tip; **`WorkspaceBar`** **`memo`**; **`ut_parameters`** ribbon + palette + **implemented** file I/O (**`design:exportParameters`** / **`design:mergeParameters`** → `output/design-parameters.json`); **PDF/DXF** export with optional **view placeholder** list from manifest (still **no** projected model geometry); **last workspace** + panel column widths in **`localStorage`**; Browser **region** / sketch point **`aria-label`s**. **Stretch (partial):** **`drawing/drawing.json`** + **`dr_base_view` / `dr_projected_view`** placeholder UX (**editable per-slot labels**, grouped fieldset, **`aria-live`** on add/remove/clear, empty-search hint in palette); **Utilities → Commands** filter **`htmlFor`/`id` wiring**, search **`aria-describedby`** catalog counts, **`aria-live`** empty-filter message; **`ut_interference`**; **Design 3D** **`ut_measure`** + **`ut_section`**; **unified 3D import** + **Fusion CSV** + **`.hsmlib` / tool file** import — [`PARITY_REMAINING_ROADMAP.md`](PARITY_REMAINING_ROADMAP.md) §Epic 7.E). **Remaining stretch:** true projected views, kernel-accurate inspect — [`PARITY_REMAINING_ROADMAP.md`](PARITY_REMAINING_ROADMAP.md) §Phase 7 |

When you finish a phase, update this table and the **Command catalog** (`Utilities → Commands`) statuses where applicable.

**Multiple agents in parallel:** see [`AGENT_PARALLEL_PLAN.md`](AGENT_PARALLEL_PLAN.md), [`agents/README.md`](agents/README.md), **per-stream done vs next** in [`agents/ALL_STREAMS_AGENT_PLANS.md`](agents/ALL_STREAMS_AGENT_PLANS.md) § *Stream status & todos*, and **ready-to-paste prompts** in [`agents/PARALLEL_PASTABLES.md`](agents/PARALLEL_PASTABLES.md).

**Verification checklists** (kernel, CAM, assembly mesh): [`VERIFICATION.md`](VERIFICATION.md).

**Docs-only maintenance** (keep this table, README, and verification prose aligned without touching code): **Stream G** — [`agents/STREAM-G-docs-only.md`](agents/STREAM-G-docs-only.md) and [`agents/PARALLEL_PASTABLES.md`](agents/PARALLEL_PASTABLES.md) → *Stream G* / *Aggressive — Stream G*.
