# CAD parity phases (implementation tracker)

Work proceeds **one phase at a time**. This file records what is done vs next.

**Detailed backlog (epics, catalog IDs, exit criteria, milestones):** [`PARITY_REMAINING_ROADMAP.md`](PARITY_REMAINING_ROADMAP.md).

| Phase | Theme | Status |
|-------|--------|--------|
| **1** | Geometry foundation — B-rep path from design, kernel manifest, CadQuery STEP+STL | **Done** — see `GEOMETRY_KERNEL.md`, `build_part.py`, **Design → Build STEP (kernel)** |
| **2** | Sketch depth — lines, arcs, trim, constraints, annotation | **Done (full pass)** — **Line** tool (two-point open polyline); **construction Point** tool (click → `points` map entry, no entity); **regular polygon** (closed point-ID polyline, 3–128 sides); **rounded slot** (`slot` entity; center–center or overall tip length + width); **polyline / rect / circle** (center+radius, **two diameter points**, or **three-point circumcircle**) **/ 3pt arc** + **center–start–end arc** (minor arc, stored as three-point `arc`); **three-point rectangle**; **trim** + **sketch fillet** + **sketch chamfer** (including arc-arc shared/near-shared endpoint fillet); **extend / break / split**; **offset** closed point-ID loop (miter, ribbon); constraints: coincident, distance, H/V, fix, perpendicular, parallel, equal, collinear, midpoint, angle, **tangent**, **symmetric**, **concentric**, **radius/diameter**; **dimension layer with hybrid auto-driving links** (linear/aligned/angular/radial/diameter); **pattern** linear/circular/path; **project/include** chain sanitization (dedupe, simplification, near-loop closure). Remaining non-Phase-2 stretch stays in [`PARITY_REMAINING_ROADMAP.md`](PARITY_REMAINING_ROADMAP.md) + [`STRETCH_SCOPE.md`](STRETCH_SCOPE.md). |
| **3** | Solid modeling on B-rep — fillet, boolean, shell, patterns on kernel bodies | **Done (kernel depth + true sweep/thread/thicken)** — includes prior `kernelOps` baseline + vNext ops (`sweep_profile_path_true`, `thread_wizard`, `thicken_offset`) with legacy-op compatibility mapping, updated samples, and schema/test coverage. |
| **4** | Surfaces / sheet metal / plastic | **Done (expanded)** — **Loft:** 2–16 closed profiles in entity order, uniform **`loftSeparationMm`**; Three.js ruled strips; kernel **union-chain** + **`loftStrategy`** (`multi+union-chain:…` when *n*≥3); sample [`resources/sample-kernel-loft-multi/`](../resources/sample-kernel-loft-multi/README.md). **Sheet:** `sheet_tab_union` baseline plus **`sheet_fold`** (bend metadata) and **`sheet_flat_pattern`** marker + manifest `flatPatternStrategy`; sample [`resources/sample-kernel-sheet-fold/`](../resources/sample-kernel-sheet-fold/README.md). **Path-guided:** `pipe_path` orientation modes + `sweep_profile_path_true` orientation-follow. **Plastic MVP:** `plastic_rule_fillet`, `plastic_boss`, `plastic_lip_groove`. |
| **5** | Assembly — joints, references, interference | **Done (full phase close)** — includes prior baseline plus solver-backed kinematics path: migrated `jointState` / `jointLimits` in schema, deterministic solved transforms with diagnostics, renderer consumption in Assembly viewport/workspace, and new IPC (`assembly:solve`, `assembly:simulate`, `assembly:interferenceCheckSimulated`) while preserving legacy preview-field compatibility. Interference/BOM exports remain available (`output/{assembly}-interference.json`, `output/bom.csv`, `output/bom-hierarchical.txt`, `output/bom-hierarchy.json`). |
| **6** | Manufacture — deeper CAM, simulation | **Done (full finish + stretch slice)** — baseline stack above plus **2D depth:** `cnc_contour` optional **multi-depth `zStepMm`**, **Make → setup context** panel (WCS/stock vs geometry), contour **bbox stats**; **drilling** doc table in [`MACHINES.md`](MACHINES.md). **Simulation (`mf_simulate` partial):** Manufacture **Tier 1** G0/G1 preview + **Tier 2** coarse 2.5D removal proxy + optional **Tier 3** experimental voxel carve sample ([`cam-voxel-removal-proxy.ts`](../src/shared/cam-voxel-removal-proxy.ts), [`ManufactureCamSimulationPanel.tsx`](../src/renderer/manufacture/ManufactureCamSimulationPanel.tsx)). **Pencil:** **`cnc_pencil`** — OCL/built-in **raster** with tighter stepover (`resolvePencilStepoverMm`). **Slicer:** merged **`-s`** map, named profiles, **`fdm_slice` → CuraEngine**; IPC **`curaEngineSettings`**. Catalog: **`mf_op_2d_*` implemented**; **`mf_op_pencil` partial**. **Stretch / not in scope:** true swept-volume boolean sim, full kinematics, turning — [`PARITY_REMAINING_ROADMAP.md`](PARITY_REMAINING_ROADMAP.md) §Phase 6 |
| **7** | Product polish — command palette, drawings, configs | **Done (baseline + discoverability polish + utilities tab a11y polish + drawing Tier A clarity)** — command palette (**workspace filter**, default **implemented-only**) + **`ut_command_palette`** (**`aria-modal`**, **Tab wrap**, **Home/End**, **PgUp/PgDn**, **scrollIntoView**, **`aria-describedby`**); empty query prioritizes **recent commands**, query supports token phrases + alias expansion (open/new/save/tools/cam/slice), non-default filters show inline **Reset filters**, and empty-match state includes **Reset palette filters**; **Shortcuts** (**`ut_keyboard_shortcuts`**, **Ctrl+Shift+?** / **⌘⇧?** when not in a typable field) from **`app-keyboard-shortcuts.ts`** + [`KEYBOARD_SHORTCUTS.md`](KEYBOARD_SHORTCUTS.md); **`ShellStatusFooter`** + dismissible palette tip; **Utilities polish:** Commands rows/groups now include keyboard-visible focus affordances, **Open command palette** CTA appears in Commands/Shortcuts, Commands search/filter state persists across refresh, Slice/CAM announce concise live status while keeping full logs readable, and the Utilities tab strip now includes **position-aware tab labels**, a visible keyboard hint, **larger touch targets**, and **horizontal small-screen scrolling**; **Drawings:** **`drawing/drawing.json`** + Project manifest; **Tier A** mesh-edge projection (`engines/occt/project_views.py`) into PDF/DXF when **`output/kernel-part.stl`** + Python succeed (**no HLR**); copy explains STL/Python fallback; **`WorkspaceBar`** **`memo`**; **`ut_parameters`** ribbon + palette + **implemented** file I/O (**`design:exportParameters`** / **`design:mergeParameters`** → `output/design-parameters.json`); **last workspace** + panel column widths in **`localStorage`**; Browser **region** / sketch point **`aria-label`s**. **Also shipped:** **Utilities → Commands** filter **`htmlFor`/`id` wiring**, search **`aria-describedby`** catalog counts, **`aria-live`** empty-filter message; **`ut_interference`**; **Design 3D** **`ut_measure`** + **`ut_section`** (kernel STL when manifest matches else preview mesh); **unified 3D import** + **Fusion CSV** + **`.hsmlib` / tool file** import — [`PARITY_REMAINING_ROADMAP.md`](PARITY_REMAINING_ROADMAP.md) §Epic 7.E). **Remaining stretch:** HLR / fully associative drawing views, B-rep–accurate inspect — [`PARITY_REMAINING_ROADMAP.md`](PARITY_REMAINING_ROADMAP.md) §Phase 7 |
| **8** | CAM depth — engagement, rest stock, rotary mesh hints | **Done (program slice + full-fat batch)** — prior slice plus **`cam:run` stock box** fields, **setup-driven `safeZMm`** / **auto raster rest** from stock + mesh min Z; **optional prior posted G-code rest floor** for mesh raster/pencil (`usePriorPostedGcodeRest` + `output/cam.nc` in Manufacture); **optional `meshAnalyticPriorRoughStockMm`** on mesh raster fallback when **no** G-code rest sampler (2.5D skip vs finish rest); **auto DOC from stock vs STL min Z** (`autoDocFromSetupMesh`); **4-axis** **`useMeshRadialZBands`** / STL-informed Z steps in `axis4_toolpath.py` plus optional **`axialBandCount`** / auto **`zStepMm`**; **cylindrical mesh raster** in main (`cam-axis4-cylindrical-raster.ts`); **Manufacture** drives CAM from the **selected** runnable `cnc_*` op with live `mfg`; **`docs/CAM_4TH_AXIS_REFERENCE.md`**; catalog + [`VERIFICATION.md`](VERIFICATION.md) CAM rows updated. **`cnc_lathe_turn`** reserved in `manufacture.json` (**Generate CAM** blocked — `MACHINES.md`). Stretch: industrial rest detection — [`PARITY_REMAINING_ROADMAP.md`](PARITY_REMAINING_ROADMAP.md) §Phase 8. |
| **9** | Trust layer — sim, envelope, feeds | **Done (program slice + full-fat batch)** — prior slice plus **Tier 3 nominal stock XY** in voxel bounds (`stockRectXYMm`); **Tier 3 voxel quality presets** (Fast/Balanced/Detailed grid + stamp budgets, browser-persisted; monotonic caps documented in catalog); sim + **Manufacture** copy stresses **approximate** tiers (Tier 2 ~88×88 height-field grid); **4-axis** sim panel note for **A** vs **`aAxisRangeDeg`**; shared **feed/plunge floor** constant (`cam-numeric-floors.ts`) kept in sync with guardrails + `calcCutParams` / material recommended-vs-clamped hints. Stretch: industrial sim — [`PARITY_REMAINING_ROADMAP.md`](PARITY_REMAINING_ROADMAP.md) §Phase 9. |
| **10** | Drawings + job documentation | **Done (program slice + full-fat batch)** — prior slice plus **Manufacture → Export setup sheet** with optional **G-code excerpt** + op order; drawing manifest **`sheetTemplateHint`** + optional **`meshProjectionTier`** (**A** edge soup / **B** + bbox-center mesh section / **C** + BRep plane section from kernel STEP when CadQuery loads) in Utilities; **HLR research** note [`docs/HLR_RESEARCH.md`](HLR_RESEARCH.md). **Not shipped:** certified OCCT HLR / associative drawing views. Stretch: production HLR — [`PARITY_REMAINING_ROADMAP.md`](PARITY_REMAINING_ROADMAP.md) §Phase 10. |
| **11** | Kernel mechanical depth | **Done (stretch batch)** — **`boolean_combine_profile`** **`extrudeDirection` ±Z**, **`pattern_path`** **`alignToPathTangent`**, samples **`features.pattern-path-tangent.example.json`**, **`features.boolean-combine-profile-minus-z.example.json`**, prior closed-path / hole-depth examples + README index; schema tests cover all **`part/*.example.json`**. Kernel failures: manifest **`userHint`** + toast guidance (`kernel-build-messages.ts`); Design 3D shows hint when manifest `ok: false`. **GEOMETRY_KERNEL.md** documents rib/web **deferral** and CAM cross-links. **Also:** **`splitKeepHalfspace`** after `split_keep_halfspace`; optional **`splitDiscardedStepPath`** / **`splitDiscardedStlPath`** when the discarded half exports; **`loftGuideRailsKernelMode`** `marker` or **`sketch_xy_align`** (bounded rail yaw on two-profile loft); manifest **`inspectBackend`** + optional **`stlMeshAngularToleranceDeg`**; rib/web **formal deferral** [`docs/RIB_WEB_SCOPE_GATE.md`](RIB_WEB_SCOPE_GATE.md). Further stretch: true rail-driven OCC loft, rib/web primitive — [`PARITY_REMAINING_ROADMAP.md`](PARITY_REMAINING_ROADMAP.md) §Phase 11. |

When you finish a phase, update this table and the **Command catalog** (`Utilities → Commands`) statuses where applicable.

**Multiple agents in parallel:** see [`AGENT_PARALLEL_PLAN.md`](AGENT_PARALLEL_PLAN.md), [`agents/README.md`](agents/README.md), **per-stream done vs next** in [`agents/ALL_STREAMS_AGENT_PLANS.md`](agents/ALL_STREAMS_AGENT_PLANS.md) § *Stream status & todos*, and **ready-to-paste prompts** in [`agents/PARALLEL_PASTABLES.md`](agents/PARALLEL_PASTABLES.md).

**Verification checklists** (kernel, CAM, assembly mesh): [`VERIFICATION.md`](VERIFICATION.md).

**Docs-only maintenance** (keep this table, README, and verification prose aligned without touching code): **Stream G** — [`agents/STREAM-G-docs-only.md`](agents/STREAM-G-docs-only.md) and [`agents/PARALLEL_PASTABLES.md`](agents/PARALLEL_PASTABLES.md) → *Stream G* / *Aggressive — Stream G*.
